import { ITexts } from "../interfaces/ITexts";

const enTexts: ITexts = {
    navbar: {
        root: "/",
        scoreboard: "/scoreboard",
        statistics: "/statistics",
    },
};

export default enTexts;
